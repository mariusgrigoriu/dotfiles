# Install GNU core utilities (those that come with macOS are outdated).
export PATH=$(brew --prefix coreutils)/libexec/gnubin:$PATH

export PATH="$HOME/bin:/usr/local/bin:/usr/local/sbin:${KREW_ROOT:-$HOME/.krew}/bin:$PATH";

